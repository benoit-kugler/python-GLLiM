(# import 'macros.tex' as macros with context #)

(# block header #)
(# endblock #)


\setlength\tabcolsep{0.07cm}
\begin{longtable}
    (# block table_def #){ l c | (! (['c c c |'] *  (hHeader | length )) | join(' ') !) }
    \cline{3 -  (!  (hHeader | length ) * 3 + 2 !) }
    (# endblock #)



    (# block header_table #)
    (# endblock #)


    \endfirsthead
    \endhead
    \endfoot
    \endlastfoot

    (#for headercell, line in zip(vHeader,MATRIX) #)

    (# if line #)
    \hline
    (# block line scoped #)

    (# for critere in CRITERES #)
    (# set rowloop = loop #)
    (# if loop.first #)

    \multirow{6}{*}{
    (# block line_first_cell scoped #)
    \makecell{ \textbf{ (! headercell.context.LABEL !) } \\
    { \small L,D = (! headercell.L !),(! headercell.D !) } \\
    {\small K = (! headercell.K !) } \\
    {\small N = (! headercell.N !) } \\
    {\small  (# if headercell.init_local #) Local = (! headercell.init_local !) (# else #) Init. standard (# endif #) } \\
    {\small Modal pred. = (! headercell.context.PREFERED_MODAL_PRED !) }}
    (# endblock #)
    }

    (# endif #)
    & (! macros.LABEL_CRITERE(critere) !)
    (# for m in line #)
    &
    (# if m and '__error__' in m #)
    (# if rowloop.first #) \multicolumn{3}{c}{\multirow{6}{*}{ (! m.__error__ !) }} (# endif #)
    (# else #)
    (# block cell scoped #)
    (! macros.MESURES(m,critere) !)
    (# endblock #)
    (# endif #)
    (# endfor #)

    (# if loop.last #) \\ (# else #) \\* (# endif #)

    (# endfor #)
    (# endblock #)
    (# endif #)
    (# endfor #)
    \hline
    \caption{\textbf{(! title !)}. (! description !)}
    \label{tab:(! label !)}
\end{longtable}


(# block footer #)
(# endblock #)
